# Generated by scripts/generate-plugin-ci-workflows.sh
name: "auth-mac-passkey windows/arm64"

on:
  workflow_dispatch:
  push:
    branches: [main, master]
    paths:
      - 'plugins/vault-plugin-auth-mac-passkey/**'
      - '.github/workflows/reusable-plugin-build.yml'
      - '.github/workflows/plugin-auth-mac-passkey-windows-arm64.yml'
  pull_request:
    paths:
      - 'plugins/vault-plugin-auth-mac-passkey/**'
      - '.github/workflows/reusable-plugin-build.yml'
      - '.github/workflows/plugin-auth-mac-passkey-windows-arm64.yml'

jobs:
  build:
    uses: ./.github/workflows/reusable-plugin-build.yml
    with:
      plugin: vault-plugin-auth-mac-passkey
      goos: windows
      goarch: arm64
