# Generated by scripts/generate-plugin-ci-workflows.sh
name: "kafka darwin/arm64"

on:
  workflow_dispatch:
  push:
    branches: [main, master]
    paths:
      - 'plugins/vault-plugin-secrets-kafka/**'
      - '.github/workflows/reusable-plugin-build.yml'
      - '.github/workflows/plugin-kafka-darwin-arm64.yml'
  pull_request:
    paths:
      - 'plugins/vault-plugin-secrets-kafka/**'
      - '.github/workflows/reusable-plugin-build.yml'
      - '.github/workflows/plugin-kafka-darwin-arm64.yml'

jobs:
  build:
    uses: ./.github/workflows/reusable-plugin-build.yml
    with:
      plugin: vault-plugin-secrets-kafka
      goos: darwin
      goarch: arm64
