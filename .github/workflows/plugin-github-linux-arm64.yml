# Generated by scripts/generate-plugin-ci-workflows.sh
name: "github linux/arm64"

on:
  workflow_dispatch:
  push:
    branches: [main, master]
    paths:
      - 'plugins/vault-plugin-secrets-github/**'
      - '.github/workflows/reusable-plugin-build.yml'
      - '.github/workflows/plugin-github-linux-arm64.yml'
  pull_request:
    paths:
      - 'plugins/vault-plugin-secrets-github/**'
      - '.github/workflows/reusable-plugin-build.yml'
      - '.github/workflows/plugin-github-linux-arm64.yml'

jobs:
  build:
    uses: ./.github/workflows/reusable-plugin-build.yml
    with:
      plugin: vault-plugin-secrets-github
      goos: linux
      goarch: arm64
