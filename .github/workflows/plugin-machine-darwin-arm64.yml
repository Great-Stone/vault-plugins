# Generated by scripts/generate-plugin-ci-workflows.sh
name: "machine darwin/arm64"

on:
  workflow_dispatch:
  push:
    branches: [main, master]
    paths:
      - 'plugins/vault-plugin-secrets-machine/**'
      - '.github/workflows/reusable-plugin-build.yml'
      - '.github/workflows/plugin-machine-darwin-arm64.yml'
  pull_request:
    paths:
      - 'plugins/vault-plugin-secrets-machine/**'
      - '.github/workflows/reusable-plugin-build.yml'
      - '.github/workflows/plugin-machine-darwin-arm64.yml'

jobs:
  build:
    uses: ./.github/workflows/reusable-plugin-build.yml
    with:
      plugin: vault-plugin-secrets-machine
      goos: darwin
      goarch: arm64
